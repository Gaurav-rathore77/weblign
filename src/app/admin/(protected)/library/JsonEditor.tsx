// The collection editor is intentionally form-based now; this compatibility
// re-export keeps older imports from breaking without exposing a JSON editor.
export { default } from './CollectionManager';
